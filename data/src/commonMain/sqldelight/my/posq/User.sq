CREATE TABLE UserData (
    userId Integer PRIMARY KEY,
    username TEXT NOT NULL ,
    fullname TEXT NOT NULL,
    email TEXT,
    phone TEXT,
    role TEXT NOT NULL,
    imageProfileUrl TEXT
);

-- Insert a single UserData record
insertUserData:
INSERT OR REPLACE INTO UserData(
    userId,
    username,
    fullname,
    email,
    phone,
    role,
    imageProfileUrl
) VALUES (?, ?, ?, ?, ?, ?, ?);

upsertUserData:
INSERT OR REPLACE INTO UserData(
    userId,
    username,
    fullname,
    email,
    phone,
    role,
    imageProfileUrl
) VALUES (?, ?, ?, ?, ?, ?, ?);

-- Get all records
selectAllUserData:
SELECT * FROM UserData;

selectUser:
SELECT * FROM UserData
WHERE userId = ?;

-- Delete a single record by id
deleteUserDataById:
DELETE FROM UserData
WHERE userId = ?;

-- Delete all records
deleteAllUserData:
DELETE FROM UserData;

